import { DisabilityRatingData } from '../types'

/**
 * Type denoting the demo data store
 */
export type DisabilityRatingDemoStore = {
  '/v0/disability-rating': DisabilityRatingData
}

/**
 * Type to define the mock returns to keep type safety
 */
export type DisabilityRatingDemoApiReturnTypes = DisabilityRatingData
