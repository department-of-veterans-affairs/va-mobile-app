import CodeBlock from '@theme/CodeBlock'
import RadioGroupString from '!!raw-loader!@componentsDocs/FormWrapper/FormFields/RadioGroup'

import ComponentTopInfo from '../../../../../src/components/ComponentInfo'

export const exampleString = `const [optionValue, setOptionValue] = useState(options[0].value)
const handleRadioOnChange = (radioValue: number): void => {
  setOptionValue(radioValue)
}\n
const options = [
  {
    value: 1,
    label: '1'
  },
  {
    value: 2,
    label: '2'
  },
]\n
return <RadioGroup<number> options={options} value={optionValue} onChange={handleRadioOnChange} />`


<ComponentTopInfo componentName="RadioGroup" example={exampleString} codeString={RadioGroupString} />
