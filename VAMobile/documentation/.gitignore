# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Automatically imported files
design/About/For\ engineers/

# The following JSON files are generated at build time by scripts in utils/ (run via `yarn prebuild`).
# They should NOT be committed - regenerate locally with `yarn prebuild` before starting the dev server.
static/data/workflows.json
static/data/github-milestones.json
static/data/whats-new-history.json

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
