name: GHA Testing

on:
  push:
    branches:
      - nr-gha-fixes

defaults:
  run:
    working-directory: VAMobile

jobs:
  cut_release_branch:
    name: Test Script
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_ACTIONS_PAT }}
      - name: Set username and email
        run: |
          git config --global user.name 'VA Automation Bot'
          git config --global user.email 'va-mobileapp@adhocteam.us'
          git tag -a release/v99.99.98 -m "Release Candidate test"
          git push origin release/v99.99.98
  # test_env_var:
  #   needs: cut_release_branch
  #   runs-on: ubuntu-latest
  #   name: Read exported variable
  #   steps:
  #     - run: |
  #         echo "$VERSION"
  #         echo "${{ env.VERSION }}"
  # android:
  #   name: Android RC Build
  #   needs:
  #     - cut_release_branch
  #     # - start_slack_thread
  #   uses: ./.github/workflows/build_android.yml
  #   secrets: inherit
  #   with:
  #     lane: rc
  #     ref: staging
  #     # slack_thread_ts: ${{needs.start_slack_thread.outputs.thread_ts}}


